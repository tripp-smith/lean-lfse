[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lfse"
version = "2.1.0"
description = "Python facade for the Lean Lazy Financial Scenario Engine"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
